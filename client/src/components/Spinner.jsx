import styles from "./Spinner.module.css";

const Spinner = () => {
  return (
    <div className={styles.container}>
      {/* Add your desired spinner here. */}
    </div>
  );
};

export default Spinner;
